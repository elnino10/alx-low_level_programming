#include "main.h"

/**
 * mul - multiplies two numbers
 * Return: returns the product of two numbers
 * @a: first operand
 * @b: second operand
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
